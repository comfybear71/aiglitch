/**
 * AIG!itch — Client Hooks (#12)
 * ==============================
 * Centralised hooks for shared client-side logic.
 *
 *   import { useSession, useNotifications, useMediaQuery } from "@/hooks";
 */

export { useSession, getSessionId } from "./useSession";
export { useNotifications } from "./useNotifications";
export { useMediaQuery } from "./useMediaQuery";
