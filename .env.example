# Anthropic API Key - Required for AI content generation
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Neon Database URL
# Automatically set when you connect a Neon Postgres store in Vercel
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Cron Secret - Set this to protect the /api/generate endpoint
# Leave empty in development to allow manual triggering
CRON_SECRET=your-cron-secret-here

# Admin password for /admin dashboard
ADMIN_PASSWORD=your-admin-password-here

# Replicate API Token - Optional, for AI image/meme generation
# Get one at https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=your-replicate-token-here

# Raphael Z-Image API Key - Optional, cheap image generation ($0.0036/image)
# Get one at https://raphael.app/z-image-api
# If not set, falls back to Replicate. Perchance (free) is always tried first.
RAPHAEL_API_KEY=your-raphael-api-key-here

# Kie.ai API Key - Optional, for AI video generation via Kling 2.6
# Get one at https://kie.ai/api-key (300 free credits on signup, no CC required)
# ~12 free videos with signup credits alone
KIE_API_KEY=your-kie-api-key-here

# Vercel Blob Token - Required for auto-syncing videos from Blob storage
# Automatically set when you connect a Vercel Blob store in your project
BLOB_READ_WRITE_TOKEN=your-blob-token-here

# Pexels API Key - Optional, free stock videos as last-resort fallback
# Get one at https://www.pexels.com/api/ (completely free, no CC required)
PEXELS_API_KEY=your-pexels-api-key-here

# xAI API Key - Optional, for Grok text generation + Aurora image generation
# Get one at https://console.x.ai/team/default/api-keys
# New accounts get $25 free credits. Supports Grok-3 text + Aurora image gen.
# If set, adds xAI Aurora to the image fallback chain and enables Grok for some AI persona content.
XAI_API_KEY=your-xai-api-key-here

# ── Solana / $GLITCH Token (Real Blockchain) ──
# Set these to enable real on-chain $GLITCH tokens via Phantom wallet

# Enable real Solana mode (set to "true" to go live)
NEXT_PUBLIC_SOLANA_REAL_MODE=false

# Solana network: "devnet" for testing, "mainnet-beta" for real launch
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta

# Premium RPC endpoint (optional, defaults to public Solana RPC)
# Recommended: Helius, QuickNode, or Alchemy for production
# NEXT_PUBLIC_SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY

# $GLITCH SPL Token Mint Address (mainnet — created 2026-02-27, 9 decimals)
NEXT_PUBLIC_GLITCH_TOKEN_MINT=5hfHCmaL6e9bvruy35RQyghMXseTE2mXJ7ukqKAcS8fT

# Treasury wallet address (holds 30M reserve tokens for new user airdrops)
NEXT_PUBLIC_TREASURY_WALLET=7SGf93WGk7VpSmreARzNujPbEpyABq2Em9YvaCirWi56

# ElonBot wallet address (42,069,000 $GLITCH — sell-restricted)
NEXT_PUBLIC_ELONBOT_WALLET=6VAcB1VvZDgJ54XvkYwmtVLweq8NN8TZdgBV3EPzY6gH

# Admin/owner wallet address (only address ElonBot can sell to)
NEXT_PUBLIC_ADMIN_WALLET=2J2XWm3oZo9JUu6i5ceAsoDmeFZw5trBhjdfm2G72uTJ

# Mint authority wallet address (Phantom — for reference only)
# MINT_AUTHORITY_WALLET=6mWQUxNkoPcwPJM7f3fDqMoCRBA6hSqA8uWopDLrtZjo

# Treasury wallet private key (for server-side token distribution / airdrops)
# WARNING: Keep this extremely secure. Never expose in client code.
# Export from CLI: solana-keygen export-secret-key ~/.config/solana/treasury.json
# TREASURY_PRIVATE_KEY=
