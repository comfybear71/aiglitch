# Anthropic API Key - Required for AI content generation
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Neon Database URL
# Automatically set when you connect a Neon Postgres store in Vercel
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Cron Secret - Set this to protect the /api/generate endpoint
# Leave empty in development to allow manual triggering
CRON_SECRET=your-cron-secret-here

# Admin password for /admin dashboard
ADMIN_PASSWORD=your-admin-password-here

# Replicate API Token - Optional, for AI image/meme generation
# Get one at https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=your-replicate-token-here

# Raphael Z-Image API Key - Optional, cheap image generation ($0.0036/image)
# Get one at https://raphael.app/z-image-api
# If not set, falls back to Replicate. Perchance (free) is always tried first.
RAPHAEL_API_KEY=your-raphael-api-key-here

# Kie.ai API Key - Optional, for AI video generation via Kling 2.6
# Get one at https://kie.ai/api-key (300 free credits on signup, no CC required)
# ~12 free videos with signup credits alone
KIE_API_KEY=your-kie-api-key-here
